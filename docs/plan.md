# План реализации (дорожная карта этапов A–G) и бэклог доработок

> Утверждённый план по документам `docs/*` + сводный бэклог доработок (замечания
> пользователя + самоотчёт агента, проверено по коду 2026-07-20 — вердикты ниже).
> Живой: статусы обновляются по мере выполнения.
> Источники: [design.md](design.md) (шаги 5–10), [dialogs.md](dialogs.md),
> [instructions.md](instructions.md), [data-store.md](data-store.md),
> [process-model.md](process-model.md), [cron.md](cron.md), [scaling.md](scaling.md),
> [openclaw-review.md](openclaw-review.md).

## Рамка

- Аутентификацию **не делаем**: она будет на уровне клиента. Ядро и API принимают
  `user_id` (непрозрачная строка, доверенная) и работают с ним. Таблица `users`,
  токены, admin-secret — отложены (см. «Дальше»).
- Порядок этапов A→F: каждый следующий опирается на предыдущий; между этапами — ревью.
- Каждый этап = код + тесты + обновление документации в одном изменении;
  `make check` зелёный в конце каждого этапа.

## Этап A. БД + диалоги (user_id, channel) + персист сообщений и задач ✅

(design.md шаг 5 без аутентификации, scaling.md этап 1, dialogs.md)

1. Зависимости: core += `sqlalchemy[asyncio]`, `aiosqlite`; web Settings +=
   `OF_DATABASE_URL` (default `sqlite+aiosqlite:///./octoforge.db`).
2. `core/src/octoforge_core/db/`: `base.py` (Base + `UTCDateTime` TypeDecorator),
   `models.py` (`dialogs`, `messages`, `tasks`; `user_id` — строковая колонка с
   индексом, без таблицы `users`), `engine.py` (async engine/session, `create_all`
   при старте; Alembic — при первой деструктивной миграции), `repositories.py`
   (DialogRepository, MessageRepository, `SqlAlchemyTaskStore`; `InMemoryTaskStore`
   остаётся для тестов).
3. Ядро: `ConversationManager` ключуется по `(user_id, channel)`; runner пересобирает
   историю из `messages`; сообщения персистятся; `SkillContext` += `user_id`, `channel`;
   `Task.conversation_id` → `dialog_id`.
4. Web API: `user_id` заголовком `X-User-Id` (400, если пустой); диалог — get-or-create:
   `POST /api/dialog/messages`, `POST /api/dialog/cancel`, `GET /api/dialog/events`;
   `/api/conversations/*` удалить.
5. UI: поле имени = `user_id` (localStorage), уходит заголовком `X-User-Id`.
6. Тесты: репозитории на SQLite `:memory:`, SqlAlchemyTaskStore, пересборка истории
   после «перезапуска»; web — get-or-create, изоляция двух `user_id`, 400 без заголовка.
7. Доки: design.md (шаг 5 → ✅, «Модель данных»/«API» — как реализовано), AGENTS.md
   (core зависит от sqlalchemy; fastapi по-прежнему запрещён).

**Проверка**: `make check`; ручной сценарий — два имени в UI, истории/задачи изолированы,
перезапуск приложения не теряет диалог.

## Этап B. Инструкции в БД + векторный поиск + external_call + SSRF-гвард ✅

(instructions.md, openclaw-review.md бэклог п.1)

**Ключевое требование — обособленный модуль.** Всё, что связано с инструкциями
(хранение, поиск, ранжирование), — самодостаточный пакет `core/instructions/`,
который легко выделить в отдельный сервис или заменить целиком. Граница модуля —
единый фасад `InstructionService` (Protocol); петля и скилы видят только его, не зная
про SQL, эмбеддинги и ранжирование. **Исполнение — на стороне core**: модуль только
хранит, ищет и ранжирует; внешние вызовы выполняет core-исполнитель поверх записей,
полученных через фасад. DTO фасада — JSON-совместимые объекты (под будущую
HTTP-границу); таблицы модуля — его собственность, вынос в сервис не тянет чужие таблицы.

1. Пакет `core/src/octoforge_core/instructions/`:
   - `api.py` — фасад `InstructionService` (Protocol) + DTO (`Instruction`,
     `InstructionType`, `SearchHit`): `search(query, k)`, `save(...)`,
     `get_by_name(name)`;
   - `local.py` + `store.py` + `models.py` + `ranking.py` — локальная реализация:
     таблица `instructions` (`knowledge|skill|tool`: id, type, title, content, embedding
     JSON, tags JSON, version, usage_count, success_count, даты), SQL-стор, cosine
     brute-force (standalone-объёмы), буст точного `title`. Полная формула openclaw
     (70/30 + MMR + затухание) — отдельной итерацией, подменой `ranking`;
   - будущая `http.py` — реализация фасада клиентом выделенного сервиса (в этап не
     входит; интерфейс сразу проектируем под неё). Выбор реализации — composition root.
2. Исполнитель внешних вызовов — **вне модуля**, в core (`net/external.py`): читает
   tool-запись через `get_by_name`, рендерит url-шаблон и параметры по схеме из записи,
   подставляет служебную авторизацию только для белого списка base-url (конфиг
   composition root), выполняет HTTP.
3. Порт `EmbeddingClient` + OpenAI-совместимый клиент `llm/embeddings.py`; модуль
   получает его конструктором (DI). Конфиг рядом с `LLMConfig`; web Settings +=
   `OF_EMBEDDING_*`. Конфиг модуля (top-k и пр.) — в Settings, пробрасывается
   конструктором, не читается из env напрямую.
4. Рантайм-тулы `instructions_search`, `instruction_save` — тонкие скилы-адаптеры над
   фасадом; `external_call` — скил над исполнителем из п.2 (фасад нужен ему только для
   чтения tool-записи).
5. SSRF-гвард (`core/net/guard.py`): resolve хоста → `ipaddress`-проверки
   (private/loopback/link-local/reserved, 169.254.169.254) → отказ; применяется в
   `http_request` и в исполнителе `external_call`.
6. Сидирование при пустой таблице: generic http tool + 1–2 скила-примера (seed-модуль
   внутри пакета, вызов в lifespan).
7. Системный промпт: правила `instructions_search`/`external_call`, когда сохранять
   новые инструкции.
8. Тесты: embeddings client (mock), локальная реализация фасада на `:memory:`
   (search/save/ранжирование/сид), исполнитель external_call (шаблоны, whitelist, блок
   SSRF с мокнутым resolver). Контрактные тесты фасада оформить так, чтобы они же могли
   проверять будущую http-реализацию.

## Этап C. Датасеты пользовательских данных ✅

(data-store.md)

1. Таблицы `datasets` (owner_user_id, name, description, schema JSON, usage_notes,
   retention, embedding, version, даты) и `dataset_records` (dataset_id FK cascade,
   owner_user_id, payload JSON, created_at).
2. Порт `DatasetStore` + SQL-реализация; валидация записи по `schema` — исполнителем.
3. Тулы `data_put` (create-if-absent), `data_query` (равенство полей, диапазон дат,
   limit), `data_forget` (каскадное удаление).
4. Дескрипторы участвуют в `instructions_search`.
5. Owner-изоляция в исполнителе по `SkillContext.user_id` (проверяет тул, не LLM).
6. Тесты: создание/валидация/фильтры, изоляция юзеров, каскадное удаление.

## Этап D. Память (per-user, кросс-поверхностная) ✅

(design.md «Модель данных», dialogs.md «факт модели»)

1. Таблица `memories` (id, user_id nullable = global, key, content, tags JSON, даты;
   unique(user_id, key)) + порт `MemoryStore` + SQL-реализация.
2. Скилы `memory_store` / `memory_search` / `memory_delete` (имена с подчёркиваниями:
   точки несовместимы с OpenAI tool-calling), `MemoryScope(USER|GLOBAL)`.
3. Промпт: когда класть/читать память; автоинъекция в контекст — отдельной итерацией.
4. Тесты: скоупы, изоляция, unique-замещение по ключу.

## Этап E. Процессная модель + LLM-роутер ✅

(process-model.md; крупный рефактор runner'а)

1. `agent/router.py`: `RouteAction` (INJECT|START_NEW|CANCEL|PROMOTE), `RouteOp`,
   `RouteDecision`, порт `MessageRouter`, `LLMRouter` (one-shot complete + tool
   `route(ops)`, валидация, таймаут, фолбэк).
2. Актор → менеджер процессов: narrative + `Process` (fg/bg), один форграунд, broadcast
   только форграунда + маркеры; пакет операций применяется по порядку.
3. `TaskStore` += `cancel`/`is_cancelled`/`count_active`; `TaskStatus` += `CANCELLED`;
   `TaskKind` → `RUN`; глобальный `TaskRunner` упраздняется, фон — pump-процессы актора.
4. `SkillContext` += порт `TaskSpawner` (лимит `OF_MAX_PROCESSES` → текст-отказ);
   детерминированный guardrail лимита в акторе.
5. События `ProcessSuspended/Resumed/Completed` → SSE + маркеры в UI.
6. Гигиена истории (openclaw-review п.4): пометка прерванного тёрна; уведомление о
   завершении фонового с защитой от двойной отправки.
7. Тесты: роутер (mock LLM: пакеты, фолбэк, passthrough), процессы
   (switch/promote/cancel/лимит/уведомления), регрессии runner'а.

## Этап F. Крон-задачи ✅

(cron.md + openclaw-review п.5–6)

1. Таблица `cron_jobs` (user_id, channel, title, schedule, timezone IANA, prompt,
   enabled, next_fire_at, last_fire_at, claimed_by, created_at) + порт `CronStore`.
2. Планировщик (asyncio-цикл, зависимость `croniter`, `zoneinfo`): выборка due,
   CAS-захват (`claimed_by` + lease TTL), coalesce пропущенных, пересчёт `next_fire_at`;
   догонялка с разбросом и лимитом реплея.
3. HTTP API (скоуп по `X-User-Id`; служебные токены — позже, с аутентификацией):
   `POST/GET/DELETE /api/cron/jobs`, `POST /api/cron/jobs/{id}/pause|resume`.
4. Сид tool-записи «cron API» в instructions → агент создаёт задачи через
   `external_call` (без выделенных скилов).
5. Выстрел: `POST /api/dialogs/{key}/wake` → фоновый процесс (Этап E) с `prompt`,
   процесс помечен id крон-задачи → уведомление; переполнение лимита → отложенное
   уведомление о невозможности запуска.
6. Тесты: расписание/таймзоны, claim CAS, coalesce, wake.

## Этап G. Telegram-адаптер ✅

(dialogs.md, design.md «Telegram-адаптер»)

1. Пакет `web/src/octoforge_web/telegram/` (core о транспорте не знает): `models.py`
   (pydantic, `extra="ignore"`, алиас `from`, `TelegramChatType(StrEnum)`), `client.py`
   (порт `TelegramClient` + `TelegramBotClient` на httpx — без aiogram), `bridge.py`
   (рендер событий runner'а в черновик с throttle-правками, статус-строки скилов,
   чанкер 4096), `poller.py` (long-poll, offset в памяти, backlog-drain, backoff) +
   `TelegramBridgeRegistry`.
2. Поверхность: канал `"telegram"`, `user_id = "tg:<telegram user id>"`, только личные
   чаты; команды `/start` и `/cancel`; идентичности web/telegram не связываются.
3. Зависимость моста — `RunnerProvider` (callable → runner); в composition root —
   `ConversationManager.get_or_create_runner`; подписка на события ДО submit.
4. Прогрев при старте: мосты для диалогов канала telegram из БД
   (`DialogRepository.list_user_ids_by_channel`) — крон-выстрелы и уведомления
   доставляются после рестарта.
5. Конфиг: `OF_TELEGRAM_BOT_TOKEN` (пусто = выключен), `OF_TELEGRAM_POLL_TIMEOUT_SECONDS`,
   `OF_TELEGRAM_EDIT_THROTTLE_SECONDS`.
6. Тесты: модели, мост (дельты → правки, статус-строки, чанкинг, отмена/ошибка),
   поллер (команды, offset, drain, восстановление, прогрев).

## Дальше (за рамками плана)

Настоящая аутентификация (`users`, токены, служебные токены для wake/cron);
distributed-профиль (scaling.md этап 2); секреты-заглушки; `GET /api/skills`,
`GET /api/tasks`; подтверждение новых скилов человеком; ~~раннее исполнение тулов
(eager tool execution)~~ — ✅ сделано вместе с idle-watchdog стрима
([streaming.md](streaming.md)); автоинъекция памяти в контекст; outbox для поверхностей.
Честный подсчёт токенов контекста, полная формула поиска (70/30 + MMR) и живучесть
задач (редоставка недоставленных результатов, реанимация/фейл осиротевших процессов)
поглощены бэклогом исследований — см. раздел ниже, п. 2, 9 и 7.

## Бэклог доработок

> Сводный список того, что надо доработать: замечания пользователя + самоотчёт агента.
> Каждый пункт самоотчёта проверен по коду (2026-07-20) — вердикт в таблице ниже.
> То, что уже отслеживается в «Дальше» выше и в [quality-audit.md](quality-audit.md),
> здесь не дублируется, а даётся ссылкой.

### Вердикты по самоотчёту агента (проверено по коду)

| Пункт агента | Вердикт | Факт по коду |
|---|---|---|
| Идемпотентность вызовов (дубли одного tool, как с напоминаниями про черешню) | **Закрыто на уровне скилла** (2026-07-20) | `cron_create` дедупит по `(title, schedule, prompt, one_shot)` → «already exists» (`core/.../skills/basic/cron_jobs.py`). Общего dedup tool-вызовов в петле по-прежнему нет — остаётся в разделе 1. |
| Обработка ошибок + ретрай крон-прогонов | **Закрыто** (2026-07-20) | Исход процесса возвращается в стор: `TaskOutcomeListener` (актор) → `CronOutcomeReporter` → `record_fire_result`; `last_status`/`last_error` в `cron_jobs`, ретрай с backoff'ом (`OF_CRON_RETRY_LIMIT`/`OF_CRON_RETRY_BACKOFF_SECONDS`). |
| Логирование и мониторинг | **Частично** | Stdlib-логирование есть (7 файлов, `basicConfig` в `web/.../main.py:201-204`). Нет JSON-логов, request-id/корреляции, метрик, трейсинга — уже отражено в [quality-audit.md](quality-audit.md) (P0-наблюдаемость, остаток на P1). |
| Rate limiting | **Подтверждено** | Лимитеров нет нигде. Есть только потолки: `agent_max_iterations=10`, `max_processes=5` на диалог, таймаут роутера, cap SSE-очереди. Уже отражено в [quality-audit.md](quality-audit.md) (S3, волна 4). |
| Стриминг ответов | **Опровергнуто — уже есть** | Web: SSE `text_delta`, UI дописывает дельты инкрементально (`web/.../api/sse.py:57-58`). Telegram: черновое сообщение с throttle-правками (`web/.../telegram/bridge.py:126-128,152-156`, `OF_TELEGRAM_EDIT_THROTTLE_SECONDS=1.5`). В бэклог не берём. |

### 1. Агент и промпты

- ~~**Путаница крон vs фоновая задача**~~ — **исправлено** (2026-07-20): правила 3/11
  системного промпта разводят `task_spawn` (работа сейчас, результат один раз) и
  `cron_create` (расписание и напоминания, включая разовые с `one_shot=true`);
  граница продублирована в описаниях скилов `task_spawn`/`cron_create` и в
  сид-сценарии `schedule_a_recurring_report`.
- **Идемпотентность tool-вызовов (dedup).** ~~Для `cron_create`~~ — **сделано**
  (дедуп по `(title, schedule, prompt, one_shot)` → «already exists»). Остаётся общий
  вариант: dedup-ключ в петле (хэш tool+args в рамках прогона, повтор → вернуть
  результат первого вызова).
- ~~**Одноразовые напоминания**~~ — **сделано** (2026-07-20): `one_shot` в `cron_jobs`
  и `cron_create`, агент строит датированное cron-выражение, задача удаляется после
  первого успешного прогона (см. [cron.md](cron.md)).

### 2. Крон

- ~~**Ретрай и наблюдаемость фейлов выстрела**~~ — **исправлено** (2026-07-20): исход
  процесса репортится актором через порт `TaskOutcomeListener` → `CronOutcomeReporter`
  → `record_fire_result`; `last_status`/`last_error`/`retry_count` в `cron_jobs`
  (видны в `cron_list` и HTTP API), ретрай с экспоненциальным backoff'ом
  (`OF_CRON_RETRY_LIMIT`=3, `OF_CRON_RETRY_BACKOFF_SECONDS`=60), CANCELLED без ретрая.
  Подробности — [cron.md](cron.md).

### 3. Telegram

- **Rich Messages.** Bot API 10.1 добавил `sendRichMessage` (нативный markdown, таблицы,
  заголовки, collapsible-блоки, чеклисты). Сейчас клиент умеет только
  `sendMessage`/`editMessageText` с HTML-разметкой (`web/.../telegram/client.py:29-48`,
  `markdown.py`); `sendPhoto`/`sendDocument`/клавиатур нет. Доработка: расширить порт
  `TelegramClient` и мост; учесть конфликт со стримингом — промежуточные `editMessageText`
  ломают rich-разметку (по опыту других проектов; вероятно, финальную версию отправлять
  rich, черновик — как сейчас).
- **Приём файлов/фото.** Сейчас нетекстовые сообщения отклоняются заглушкой «Пока понимаю
  только текстовые сообщения» (`web/.../telegram/poller.py:113-115`; в моделях нет полей
  photo/document — `telegram/models.py:35-43`). Доработка: модели под photo/document/voice,
  `getFile` + скачивание, подача в контекст (vision/текстовое описание), лимиты размера.

### 4. UX и интеграции

- **Автоопределение языка ответа.** В системном промпте нет указания отвечать на языке
  пользователя — вероятно, достаточно одной строки в промпт; проверить на практике.
- **Интеграция с календарями** (Google/Apple Calendar) для напоминаний и планирования.
  Зависит от аутентификации/OAuth (см. раздел 5).
- **Дашборд** — веб-интерфейс просмотра данных: диалоги, задачи, cron-задачи, память,
  датасеты. Частично упирается в отсутствующие GET-эндпоинты (`GET /api/skills`,
  `GET /api/tasks` — уже в «Дальше» выше).
- ~~Стриминг ответов~~ — уже реализован на обеих поверхностях (см. таблицу вердиктов).

### 5. Безопасность и эксплуатация

- **Аутентификация** — уже в «Дальше» выше и в [quality-audit.md](quality-audit.md)
  (S1, волна 4): users, токены, служебные токены для wake/cron. Дополнительно: allowlist
  telegram user id — сейчас бот отвечает любому аккаунту, который его нашёл
  (`web/.../telegram/poller.py:119-125`). Подробный план (инвайты по кодам без HTTP,
  админский тул `admin_manage`, полностью изолированный от `core/`) — зафиксирован в
  [telegram-invites-plan.md](telegram-invites-plan.md), реализация отложена.
- **Хранилище секретов.** Токены уже в `.env` (файл `OctoForgeBotToken.txt` из корня
  убран); дальше — keychain/sops/age. Частично
  пересекается с [quality-audit.md](quality-audit.md) (P1 — операбельность).
- **Версионирование инструкций.** Откат скиллов/инструкций, если новая версия сломалась:
  версии записей в таблице `instructions`, активная версия, откат = переключение активной.
- **Наблюдаемость P1** (структурные JSON-логи, request-id, метрики) и **rate limiting** —
  уже отслеживаются в [quality-audit.md](quality-audit.md) (остаток P0-наблюдаемости, S3,
  волны 2–4), здесь не дублируем.
- ~~**Компакция контекста**~~ — **сделано** (2026-07-20): ветка процесса = системный
  промпт + блок саммари тем + горячий хвост; фоновая LLM-суммаризация при переполнении
  хвоста (`OF_CONTEXT_HOT_MAX_CHARS`), поиск по архиву — скилом `history_search`.
  Подробности — [context.md](context.md). Честный подсчёт токенов и оглавление тем —
  отдельными итерациями (см. «Не входит» там же).

### Предлагаемый порядок

> На 2026-07-21 первым по значимости идёт «Бэклог по итогам трёх исследований»
> (раздел ниже): его пункты подтверждены тремя независимыми проектами и закрывают
> риски надёжности и стоимости хостеда — раньше новых поверхностей из пп. 3–6.

1. ~~Промпт крон/фон + dedup `cron_create`~~ — ✅ сделано (2026-07-20).
2. ~~Компакция контекста / окно нарратива~~ — ✅ сделано (2026-07-20): модуль
   `context/` (саммари в `dialog_summaries` + горячий хвост), скил `history_search`,
   см. [context.md](context.md).
3. Telegram: rich messages, приём файлов/фото.
4. Наблюдаемость P1 (+ ~~ретрай крон-выстрелов~~ — ✅ сделано 2026-07-20).
5. Аутентификация (с allowlist telegram).
6. Календари, дашборд, версионирование инструкций.

## Бэклог по итогам трёх исследований (hermes / opencode / openclaw)

> Свод рекомендаций, подтверждённых **всеми тремя** сравнительными исследованиями
> независимо друг от друга: [hermes-review.md](hermes-review.md),
> [opencode-review.md](opencode-review.md), [openclaw-review.md](openclaw-review.md).
> Тройное подтверждение = это общие закономерности зрелых агентов, а не чья-то
> особенность. Порядок пунктов — по зависимостям, не по приоритету; порядок
> исполнения — в конце раздела. Каждый пункт: что делаем, зачем (какой сценарий
> поломки или переплаты закрывает), как это решено у референсов, стоимость.

### 1. Таксономия ошибок LLM + ретраи с классификатором и Retry-After ✅

> **Сделано** (2026-07-21): `llm/errors.py` (таксономия + классификация из статуса/тела,
> Retry-After), `RetryingLLMClient` в `build_llm_client` (backoff+jitter, стрим — до
> первого события), событие `RetryScheduled` → LoopEvent → SSE/Telegram/UI, конфиг
> `OF_LLM_MAX_RETRIES`/`OF_LLM_RETRY_BASE_SECONDS`/`OF_LLM_RETRY_MAX_SECONDS`.
> См. [design.md](design.md) «Ошибки LLM и ретраи».

- **Что.** Новый модуль `core/src/octoforge_core/llm/errors.py`: типизированная
  таксономия (`rate_limit(retry_after) | auth | quota | context_overflow |
  provider_internal | transport`), классификация из HTTP-статуса и тела ответа в
  `llm/openai.py` вместо голого `raise_for_status()`. Поверх порта `LLMClient` —
  декоратор `RetryingLLMClient` (оборачивает любую реализацию, wiring только в
  composition root): экспоненциальный backoff с jitter, задержка не ниже
  `Retry-After`, ретраим только транзиентные классы (rate_limit/provider_internal/
  transport), лимит попыток в `LLMConfig`. Новое событие `RetryScheduled`, чтобы
  web/Telegram могли показать «повторная попытка» вместо молчания.
- **Зачем.** Сегодня любой транзиентный сбой — 429 при всплеске наших же
  крон-прогонов, 502 у провайдера, обрыв соединения — убивает прогон целиком:
  пользователь получает ошибку вместо ответа, а ночная крон-задача молча уходит в
  FAILED до следующего слота. Это самая частая причина видимых сбоев hosted-сервиса
  на одном API-ключе: наши же процессы (web + telegram + крон + фоновые задачи)
  конкурируют за один rate limit. Все три проекта независимо пришли к одной форме
  решения — это закономерность, а не совпадение.
- **Как у других.** openclaw — трёхслойно: классификатор транзиентности
  `src/provider-runtime/operation-retry.ts` (500/502/503/504/DNS/timeout — да;
  400/401/403/404 — нет) + generic `packages/retry` (backoff, full-jitter, пол по
  Retry-After). opencode — `packages/ai/src/provider-error.ts` (статус + коды +
  ~25 regex'ов тела, отдельный подкласс `context-overflow`) и `session/runner/
  retry.ts` (≤4 попыток, событие `RetryScheduled`). hermes — `agent/retry_utils.py`
  (decorrelated jitter) + адаптивные тиры для перегруженных провайдеров.
- **Стоимость:** ~1–2 дня (таксономия ~150 строк + декоратор ~80 строк + тесты).
  Основа для п. 2 (класс `context_overflow`) и будущего `FallbackLLMClient`.

### 2. Usage capture → токенный триггер компакции ✅

> **Сделано** (2026-07-21): `stream_options.include_usage`, DTO `Usage` в
> `StreamFinished` и `Completion` из `complete()` (порт изменился, фейки обновлены),
> колонки `prompt_tokens`/`completion_tokens` на `messages` (миграция `4c7e2b9a1f63`),
> второй триггер компактора `prompt_tokens ≥ OF_MODEL_CONTEXT_TOKENS −
> OF_CONTEXT_BUFFER_TOKENS` (0 = выключен; символьная эвристика — фолбэк).
> См. [context.md](context.md) и [design.md](design.md) «Usage capture».

- **Что.** Просим usage у провайдера (`stream_options={"include_usage": true}` в
  `llm/openai.py`), DTO `Usage` (input/output/cached) добавляется в `StreamFinished`
  и результат `complete()`, персистится на сообщении (миграция). Компактор получает
  второй триггер: `prompt_tokens последнего прогона ≥ OF_MODEL_CONTEXT_TOKENS −
  OF_CONTEXT_BUFFER_TOKENS`; символьная эвристика остаётся фолбэком, когда usage нет.
- **Зачем.** Символьный прокси chars/4 систематически врёт: кириллица и код дают
  токен-рейты, далёкие от 4:1, а системный промпт, схемы скилов и блок тем вообще
  не входят в подсчёт хвоста. Итог — компактируем либо поздно (провайдер отвечает
  hard-fail, прогон теряется), либо рано (жжём деньги на лишние суммаризации).
  Попутно это фундамент per-user учёта стоимости — без него hosted-сервис не знает,
  кто сколько сжёг, и не может вводить квоты.
- **Как у других.** openclaw — `src/agents/usage.ts`: `normalizeUsage` сводит формы
  всех провайдеров (включая cacheRead/cacheWrite и коррекцию двойного счёта),
  `deriveContextPromptTokens` — честный размер промпта. opencode —
  `session/usage.ts`: usage на assistant-сообщении, проектор накапливает суммы на
  сессии. hermes — `agent/context_engine.py`: порог = доля окна модели от
  `prompt_tokens` ответа.
- **Стоимость:** ~1–2 дня (порт + клиент + миграция + триггер). Зависит от п. 1
  (классификация ошибок), кормит п. 3 и будущие квоты.

### 3. Реактивная компакция при context-overflow + rolling-merge саммари ✅

> **Сделано** (2026-07-21): порт `ContextCompactor.compact_now` + ретрай прогона
> в раннере (пересборка ветки, один повтор, повторный overflow → `Failed`);
> rolling merge — промпт обновляет предыдущее саммари (структура
> Goal/State/Next/Decisions, точные идентификаторы), `replace_for_dialog`
> держит одну запись на диалог (блок тем константного размера; цена — ≤4 тегов
> и грубый `find_by_topic`). См. [context.md](context.md).

- **Что.** Провайдер вернул `context_overflow` (класс из п. 1) → синхронная
  компакция через существующий порт `ContextCompactor` → один повтор прогона;
  повторный overflow = честная ошибка. Промпт суммаризации (`context/prompts.py`)
  получает предыдущее саммари и обновляет его (rolling merge), а не пишет новое с
  нуля; шаблон становится структурным (цель / состояние / следующий шаг / решения +
  правило «сохраняй точные идентификаторы: пути, команды, тексты ошибок») с
  сохранением наших тегов тем.
- **Зачем.** Сегодня переполнение контекста = `Failed` и потерянный прогон: у
  пользователя просто ошибка, хотя механика восстановления у нас уже есть. А
  append-only саммари — это признанный в `docs/context.md` риск «сотни тем»:
  блок тем растёт без bounds и со временем сам съедает контекст. Rolling merge
  держит размер блока константным.
- **Как у других.** opencode — `session/compaction.ts`: anchored summary («update
  the anchored summary… preserve still-true, remove stale, merge new») и рестарт
  шага после компакции; триггер от реального usage против `context_limit − buffer`.
  openclaw — overflow уходит не в фейловер модели, а в компакцию-восстановление.
  hermes — итеративное обновление предыдущего саммари + структурный шаблон
  (Active Task verbatim / Decisions / Pending).
- **Стоимость:** ~2–3 дня. Зависит от п. 1–2 (нужны класс ошибки и usage).

### 4. Идемпотентный submit по client_message_id ✅

> **Сделано** (2026-07-21): `submit(content, client_message_id=None)`, колонка
> `messages.client_message_id` + unique `(dialog_id, client_message_id)`
> (миграция `8a1f3d5c2e97`, NULL'ы сосуществуют), skip-if-seen в акторе
> (повтор — ни персиста, ни роутинга), `POST /api/dialog/messages` принимает
> ключ, Telegram-адаптер шлёт `update_id` как ключ. См. [design.md](design.md)
> «API».

- **Что.** Опциональное поле `client_message_id` в `POST /api/dialog/messages` и
  `ConversationRunner.submit()`; колонка на `messages`, unique-index
  `(dialog_id, client_message_id)`, повтор с тем же ключом → вернуть существующее
  (skip-if-seen в акторе). Telegram-адаптер использует `update_id` как ключ.
- **Зачем.** Ретраи доставки — норма, а не исключение: Telegram шлёт update повторно,
  пока не получит 200; HTTP-клиент ретраит при таймауте, не зная, дошёл ли запрос.
  Сегодня каждый такой ретрай задваивает user-сообщение и прогон — двойная трата
  LLM и размноженные ответы в истории.
- **Как у других.** openclaw — `src/sessions/user-turn-transcript.ts`: user-turn
  хранит `idempotencyKey`, запись дедупит сканом по ключу. opencode —
  `session/pending.ts`: admission идемпотентен по client-supplied id. hermes —
  эквивалентная защита на уровне delivery ledger (обязательство создаётся один раз).
- **Стоимость:** ~полдня (миграция + проверка в акторе + тесты).

### 5. Prefix-cache-дисциплина промпта ✅

> **Сделано** (2026-07-21): `_with_current_date` удалён — дата была с точностью
> до минуты и била prefix-cache каждую минуту каждого процесса. Системный
> промпт байт-стабилен; дата/время — конвертом на последнем сообщении ветки
> (`_with_date_envelope`, в т.ч. фоновые задачи и пересборка после реактивной
> компакции); нарратив и персист — чистая копия. `prompt_cache_key` отложен
> (ollama не поддерживает; решение зафиксировано в
> [prompt-caching.md](prompt-caching.md)).

- **Что.** Убрать `_with_current_date` из системного промпта: дата/время — в конверт
  последнего сообщения ветки (user/system-обёртка), в промпте остаётся только
  таймзона. `prompt_cache_key = dialog_id` там, где endpoint это honour'ит.
  Позже — `cache_control`-брейкпоинты на границе «system + блок тем | хвост»,
  когда появится Anthropic-совместимый бэкенд.
- **Зачем.** Системный промпт + блок саммари тем — самый большой и самый
  стабильный кусок контекста. Сегодня каждая новая ветка процесса (каждый ответ,
  каждый крон-выстрел, каждая фоновая задача) начинается со свежей датой в
  промпте — и бьёт prefix-cache провайдера целиком: платим за полный ввод на
  каждом вызове каждого процесса. На OpenRouter/Anthropic cache-read стоит в
  разы дешевле полного ввода — это немедленная экономия денег, а не оптимизация
  на вырост.
- **Как у других.** openclaw — маркер `SYSTEM_PROMPT_CACHE_BOUNDARY`
  (`src/agents/system-prompt.ts`): стабильный префикс отдельно, динамика ниже;
  текущее время сознательно отсутствует в системном промпте и ездит в конверте
  сообщения. opencode — `packages/ai/src/cache-policy.ts`: hints на последний тул,
  последний system-блок, последнее user-сообщение. hermes —
  `agent/prompt_caching.py`: стратегия «system + 3 последних», 4 брейкпоинта.
- **Стоимость:** ~полдня–день (конверт + конфиг + тесты). Дёшево, отдача в деньгах
  немедленная.

### 6. Utility-модель для роутера и компактора

- **Что.** Второй `LLMConfig` (`OF_LLM_UTILITY_MODEL` + при желании отдельный
  base_url/key, дефолт = основная модель); composition root строит второй клиент и
  инъектит его в `LLMRouter` и `LlmContextCompactor`. Порты не меняются.
- **Зачем.** Роутер вызывается на **каждое** входящее сообщение пользователя,
  компактор — регулярно на каждый диалог. Обе задачи — одношаговая классификация/
  суммаризация, с которыми дешёвая модель справляется с запасом; сегодня обе
  работают на самой дорогой модели в системе. Это самая высокая цена/стоимость
  среди всех пунктов: конфигурационное изменение с прямым эффектом на счёт.
- **Как у других.** openclaw — `src/agents/utility-model.ts`: `utilityModel` из
  манифеста провайдера, используется для лейблов/наррации/подзадач. hermes —
  `agent/auxiliary_client.py`: центральный роутер сайд-задач с per-provider
  картой дешёвых моделей (haiku/flash). opencode — отдельная title-модель для
  генерации заголовков сессий.
- **Стоимость:** ~часы (второй конфиг + два конструктора + тесты wiring'а).

### 7. Startup-recovery осиротевших задач

- **Что.** При старте: задачи в PENDING/RUNNING (исполнитель — in-memory процесс,
  умерший с сервисом) переводятся в FAILED с `last_error="interrupted by restart"`,
  в нарратив диалога — системная заметка; результаты с `result_delivered=false`
  редоставляются (репорт-прогон или инъекция). Реализация — порт-методы на
  `TaskStore` (`fail_orphaned()` / `list_undelivered()`), вызов из lifespan рядом с
  прогревом telegram-мостов.
- **Зачем.** Процессы живут в памяти: рестарт сервиса молча сиротит все RUNNING.
  Крон-задача остаётся «висеть» в RUNNING навсегда (ретрай не наступит — ретраит
  только FAILED), пользователь не узнаёт, что его фоновая работа умерла. Это
  единственный класс потери данных, который у нас остаётся сознательно открытым
  (AGENTS.md «Не реализовано»).
- **Как у других.** opencode — `session/execution/restart.ts`: suspend при
  shutdown, `consumeSuspended` CAS-UPDATE при старте → at-most-once резюм (самая
  чистая форма; для полного resume позже — порт-методы suspend/consume).
  openclaw — `src/cron/service/startup-run-repair.ts`: stuck `runningAtMs` → failed
  с пометкой «interrupted by gateway restart». hermes — delivery ledger: sweep при
  старте, redelivery с честным маркером «♻️ recovered».
- **Стоимость:** ~1–2 дня (порт + sweep + заметка + тесты). Поглощает пункт
  «живучесть задач» из «Дальше».

### 8. Порт SkillHooks → аудит metadata-only

- **Что.** Порт `SkillHooks` (`before(skill, args, ctx) -> args | Blocked`,
  `after(skill, result, ctx) -> result`), вызывается в лупе при исполнении каждого
  скилла, цепочка собирается в composition root (дефолт — no-op). Первый
  потребитель — пакет `audit/` за портом `AuditStore`: события
  `skill.action.finished` (user_id, dialog_id, skill, статус, размер результата)
  **без args/result**, asyncio-очередь в SQLite.
- **Зачем.** Мультиюзерный хостед без аудита слеп: «кто ночью дёргал
  `external_call`?», «какой диалог сжёг квоту?», «почему у пользователя удалился
  датасет?» — сейчас не ответить. Это форензика злоупотреблений и база для
  будущих per-user лимитов. А сам шов хуков — необходимая точка для следующего
  шага (approval-гейт деструктивных скилов, центральная валидация аргументов,
  детектор петель) — все они живут в одном месте вызова.
- **Как у других.** openclaw — `src/audit/`: metadata-only события, идентификаторы
  псевдонимизированы HMAC, выделенный писатель с bounded-очередью; плюс хуки
  `before_tool_call` (block/rewrite/requireApproval). opencode —
  `tool/hooks.ts`: мутабельные before/after события, plugin-регистрация.
  hermes — `agent/tool_guardrails.py`: чистый контроллер на пути вызова тулов
  (детектор петель) — доказывает, что один шов покрывает разные цели.
- **Стоимость:** ~2–3 дня (шов в лупе + `AuditStore` + очередь + тесты).

### 9. Гибридный поиск в memory/ + версия эмбеддинг-модели

- **Что.** Колонка `embedding` в `memories` (наш существующий `EmbeddingClient`),
  поиск = мердж `0.7*cosine + 0.3*keyword(LIKE)` с сохранением буста точного
  ключа — drop-in замена ранжирования по образцу `instructions/ranking.py` (там
  формула уже заложена комментарием). Колонка `embedding_model`; при рассинхроне с
  `EmbeddingConfig` — статус «reindex needed» и ленивое перевычисление при доступе.
  Capability-порт `MemoryVectorSearch` — по образцу `InstructionVectorSearch`.
- **Зачем.** LIKE ищет только дословные подстроки: сохранил «любимый фильм —
  Бегущий по лезвию», спросил «какое кино я люблю» — не нашёл. Память, которая не
  находится по смыслу, хуже отсутствия памяти: агент уверен, что фактов нет, и
  переспрашивает. Отдельная мина — смена эмбеддинг-модели: векторы разных моделей
  несравнимы, а сегодня мы бы молча смешали их в одном поиске (уже актуально для
  `instructions`, где эмбеддинги есть, а версионирования нет).
- **Как у других.** openclaw — `extensions/memory-core/src/memory/hybrid.ts`:
  вектор ∥ FTS5 BM25, `score = 0.7*vector + 0.3*keyword`, пул кандидатов 4×,
  буст точных имён; `manager-reindex-state.ts` — честный статус «index was built
  for model X, expected Y». hermes — FTS5 + BM25 + сниппеты по сессиям.
  opencode — памяти нет, но их вывод про `search_fts` за портом подтверждает
  направление «поиск за capability-портом».
- **Стоимость:** ~2–3 дня (миграция + ранжирование + версия + тесты). Поглощает
  пункт «полная формула поиска (70/30 + MMR)» из «Дальше».

### 10. Финал при max-iterations вместо Failed

- **Что.** При срабатывании лимита итераций лупа — дополнительный вызов с
  `tools=[]` и инструкцией «подведи итог»; его результат — финал прогона вместо
  голого `Failed(MAX_ITERATIONS_MESSAGE)` (`agent/loop.py`).
- **Зачем.** Сейчас при лимите пользователь получает голую ошибку, а tool-трасса
  итераций остаётся только в ветке — работа прогона теряется из виду.
- **Как у других.** opencode (волна 1, п. 2 ревью).
- **Стоимость:** ~полдня (одна ветка в лупе + промпт + тесты).

### 11. FallbackLLMClient — фейловер на запасной endpoint

- **Что.** Декоратор над `LLMClient` (по образцу `RetryingLLMClient`): при фейле
  до первого события стрима — переключение на запасной endpoint/модель
  (второй `LLMConfig`, дефолт — без фейловера). Wiring в composition root.
- **Зачем.** Один endpoint упал = весь сервис стоит: ни диалоги, ни крон.
  Ретраи (п. 1) спасают от транзиентов, но не от длительного аутеджа провайдера.
- **Как у других.** openclaw — fallback-цепочка провайдеров до первого токена.
- **Стоимость:** ~день (декоратор + конфиг + тесты).

### 12. Watchdog процесса целиком

- **Что.** `OF_PROCESS_IDLE_TIMEOUT`: процесс, не шлющий события N секунд
  (например, скил завис в `await`), отменяется с FAILED-заметкой. Существующий
  `OF_LLM_STREAM_IDLE_TIMEOUT` покрывает только ожидание событий LLM.
- **Зачем.** Зависший скил держит процесс (и слот лимита) вечно — пользователь
  ждёт ответ, который никогда не придёт.
- **Как у других.** openclaw (agent, п. 4 ревью).
- **Стоимость:** ~полдня–день (таймер на pump-задачу + заметка + тесты).

### 13. Ограничения task_spawn: depth-guard и бюджет результата

- **Что.** `OF_MAX_SPAWN_DEPTH` (bg-процесс получает `task_spawner` и может
  спавнить безгранично — сейчас сдерживает только общий лимит процессов) и
  `OF_TASK_RESULT_MAX_CHARS` (результат задачи уходит в нарратив по
  `TASK_DONE_TEMPLATE` без усечения).
- **Зачем.** Рекурсивный спавн — неконтролируемый расход; гигантский результат
  фоновой задачи раздувает нарратив и давит компактор.
- **Как у других.** Отмечено всеми тремя ревью (★ волна 1).
- **Стоимость:** ~часы (конфиг + проверка в спавнере + усечение + тесты).

### 14. Фейлы — данными нарратива

- **Что.** При `Failed` прогона в нарратив пишется системная заметка (по образцу
  `INTERRUPTED_NOTE`): факт сбоя и класс ошибки.
- **Зачем.** Сейчас при фейле в нарратив не пишется ничего: user-сообщение
  остаётся «провисшим», роутер и компактор не видят, что на вопрос уже отвечали
  и упали — при рестарте диалога контекст искажён.
- **Как у других.** openclaw (agent, п. 5 ревью).
- **Стоимость:** ~часы (заметка + тесты; учесть, что заметка не должна провоцировать
  повторные ответы).

### 15. Компакция: keep-recent пол, cooldown, оверсайз-защита, ручной триггер

- **Что.** (a) keep-recent пол — в хвосте всегда остаются последние N сообщений
  (сейчас гарантировано лишь последнее одно); (b) cooldown/счётчик неэффективных
  компакций — повторный фейл суммаризации сейчас жжёт LLM-вызов на каждом
  `assemble`; (c) защита от оверсайз-сегментов — одно гигантское сообщение целиком
  уходит в `complete()` суммаризатора → он сам ловит overflow → вечный soft-fail
  (заглушка `[Large message omitted]` или чанкинг); (d) ручной триггер — скил
  `history_compact` поверх готового `compact_now`.
- **Зачем.** (a)-(c) — устойчивость компактора к патологическим диалогам;
  (d) — дешёвый пользовательский контроль («сожми контекст»).
- **Как у других.** openclaw — `keepRecentTokens`/`recentTurnsPreserve`,
  `summarizeInStages`, заглушки; hermes — cooldown, последний user всегда в хвосте.
- **Стоимость:** ~1–2 дня (все четыре подпункта в `context/` + тесты).

### 16. Мелкие наблюдения аудита 2026-07-22 (по мере касания)

- Debounce/склейка всплеска `_Submit` в акторе перед вызовом роутера (openclaw,
  волна 1 п. 9) — экономит вызовы роутера на «пулемётном» вводе.
- Drop-and-close для переполненного подписчика broadcast (сейчас — дроп со
  счётчиком, подписчик не узнаёт о дыре; opencode, волна 1 п. 3).
- First-token timeout отдельно от idle (reasoning-модели думают дольше idle).
- Ретрай пустых ответов (`Failed(empty stream)` — сейчас без единой попытки).
- Lifecycle инструкций (hermes): `archived`/`pinned`, patch-операция вместо
  полной замены в `instruction_save`, конвенция структуры сценария.
- Метрики инструкций: `success_count` не имеет писателя, `usage_count` меряет
  показ, а не использование — определить семантику до реализации usage-aware ранга.

### Порядок исполнения

- **Сделано:** п. 1–5 (ошибки/ретраи, usage, реактивная компакция,
  идемпотентность, prefix-cache) + аудит-фиксы 2026-07-22 (28 пунктов по
  agent/llm/context/instructions).
- **Сначала дёшево и независимо:** п. 6 (utility-модель), п. 13, п. 14, п. 10 —
  часы-полдня каждый.
- **Затем фундамент:** п. 7 (startup-recovery), п. 15 (компакция), п. 11
  (fallback) — надёжность.
- **Параллельно можно:** п. 8 (hooks + аудит), п. 9 (гибридный поиск), п. 12
  (watchdog) — ни от чего не зависят.

## Принятые решения по умолчанию

- ORM и репозитории — в `core/db/`; core получает зависимость sqlalchemy (fastapi —
  по-прежнему запрещён).
- `user_id` — доверенная строка от клиента (`X-User-Id`); изоляция логическая, без
  криптографии — до настоящей аутентификации.
- `create_all` вместо Alembic до первой деструктивной миграции.
- Outbox откладывается до второй поверхности (в standalone уведомления in-process).
- Порядок B→C→D→E→F: инструкции/датасеты/память раньше роутера (в docs «отложен»),
  крон после процессной модели (выстрел = фоновый процесс).
